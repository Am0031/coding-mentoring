{{#each data}}
  <div class="framework-cb form-check ml-3 mr-3">
    <input
      class="form-check-input"
      type="checkbox"
      value={{frameworkName}}
      id={{id}}
    />
    <label class="form-check-label" for="flexCheck">
      {{frameworkName}}
    </label>
  </div>
{{/each}}